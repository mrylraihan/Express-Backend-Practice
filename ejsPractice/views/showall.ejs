<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>show all</title>
</head>
<body>
    <ul>
        <% fruits.forEach(ele=>{ %>
            <li><%= ele.name %></li>
       <% }) %>
    </ul>
    <ul>
        <% fruits.forEach((ele,idx)=>{ %>
            <li>
                <a href="/fruits/<%=idx%>"><%= ele.name %></a>
            </li>
       <% }) %>
    </ul>
</body>
</html>